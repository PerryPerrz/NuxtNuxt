import { addRouteMiddleware } from "nuxt/app"

export default defineNuxtPlugin(() => {
  /*
    addRouteMiddleware('global-test', () => {
      //console.log('Global middleware, méthode 1')
    }, { global: true })*/
  })